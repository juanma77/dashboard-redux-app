import { IncomeOutcomeOrderPipe } from './income-outcome-order.pipe';

describe('IncomeOutcomeOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeOutcomeOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
